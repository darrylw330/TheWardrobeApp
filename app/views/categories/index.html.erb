for each category
  get the clothings that belong to that category AND filter by current user's ID
  then choose one random clothing and show it

  # a_category.clothings.where(user_id: @current_user.id).sample
